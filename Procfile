# Specifies the Python version to use
python-3.9.0

# Installs any dependencies specified in requirements.txt
pip install -r requirements.txt

# Runs the application when the server starts
web: python app.py
